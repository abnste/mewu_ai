# 发布文档与配图

- Provider 的非成功 HTTP 响应可能回显提示词、媒体 Data URL 或认证信息。统一经 `AI/ProviderHttpError.cs` 在 16 KiB 上限内解析，只允许显示固定分类、白名单参数名、有限数值服务码和严格校验的追踪编号；不得恢复原始错误正文或请求内容。MiniMax M3 的视频请求遵循 `video_url`、Base64、`fps`、流式和思考字段的正式 OpenAI-compatible 规范，HTTP 422 必须保留服务端安全诊断后再归因，不能只根据视频时长、容器或文件大小猜测。

- README 面向首次使用软件的用户：用自然、直接的语言介绍产品、演示、下载安装、支持的 AI 和常见问题。不得把“本机文字”“合成挑战”“协议握手”“验证范围”“接口边界”等研发报告用语放进首页。测试证据与接入实现留在专题文档；版本改动留在 CHANGELOG / release notes。重要限制应转成用户能采取行动的说明，不能靠删掉限制来夸大能力。中英文分别自然撰写，保留现有演示图、GIF、版本下载入口与历史链接。

- v0.3.0 已于 2026-09-07 正式发布，标签提交 `55eb0bd`；完整云端预检 `34088371056` 与正式标签构建 `34088687286` 全部成功，本地 Release 848 项通过、1 项真实音频设备验收跳过。GitHub latest 为 v0.3.0，发布说明与标签文档正文一致；中英文 README 下载链接和完整历史索引已验证。实际下载安装 EXE 92,795,194 字节（SHA-256 `78c22847c8fb1fbf0b96486427559773a02c9016514fe0371561ec6a66d71d07`）、便携 ZIP 122,826,194 字节（`f5dda382bc7d6f4319ae8bbaac251ed2232a917a8c6d1ebba11304024dbac1ad`），均与 GitHub digest 一致。安装器及 ZIP 内程序集版本均为 0.3.0.0，ZIP 许可与敏感/开发文件排除审计通过；验收未执行安装器。原 11 个正式版本的标签说明均可访问，v0.2.6 原文已恢复，后续附件清理以独立日期说明呈现。此记录为发布后文档，不移动 v0.3.0 标签。

- v0.3.1 已于 2026-09-08 正式发布，标签源码提交 `3e9ce47`；云端预检 `34146444230` 与正式标签构建 `34146734743` 全部成功。Release 只上传 `MewuAI-Setup-0.3.1-win-x64.exe`（92,776,268 字节，GitHub digest `182dbf0a06c3d99f299be53bac143fa358a49a20dfd7c5c8293971c86afdcc8c`）和 `MewuAI-Portable-0.3.1-win-x64.zip`（122,830,976 字节，digest `a06d2f88e998bed7843f229f22b5640f16232b4898dc3d4f40c5d42d768f412c`）。GitHub latest 已指向 v0.3.1；不得为补记这条发布记录移动标签或替换资产。

- 2026-09-07 发布纠正：完整核对 `v0.2.6..HEAD`，以 0.3.0 汇总 Codex / WorkBuddy / MiniMax Code、多 API 与模型切换、纯文字/JSON/思考修复、MPL-2.0 和界面更新。版本必须同时更新项目、安装器、双语 README、发行说明和测试夹具；先通过同一提交的云端全流程预检，再创建新标签并等待安装包发布完成。
- 发行说明按该标签实际代码撰写，禁止用新功能或文档清理覆盖旧版说明；`CHANGELOG.md` 链接全部正式版本与原始标签说明，另列失败或未发布记录。版本管理包含下载包版本与用户可见更新记录，不能仅推送 master 后声称已发布新版。
- v0.2.6 原始发行说明已恢复到标签内容；2026-09-07 删除旧校验资产是后续维护事件，不能改写为“发布时未附带”。新 Release 不附 SHA256SUMS.txt，README 和新版本说明须明确提醒 0.2.5 及更早客户端手动下载升级，不能隐藏其更新器依赖旧校验文件的事实。

- `workbuddy-integration.md` 记录本机 WorkBuddy 5.5.3 官方 ACP 接入与真实合成挑战。文字、图片及 Agent 自行处理 MP4 已通过；视频使用本机现有工具，不宣称每台电脑都有相同解码工具，也不声称腾讯承诺桌面捆绑 CLI 路径永久稳定。此次为本地适配，版本仍为 0.2.6，不替换既有 GitHub Release。

- 2026-09-06 用户明确选择 MPL-2.0，取代此前笼统的“商业使用需作者授权”。根 LICENSE 使用 Mozilla 官方原文，SOURCE.md 说明项目源码范围、源码获取与第三方例外；README 以 MPL-2.0 徽章和链接展示。此许可提交不复用发布标签、不替换已发布 v0.2.6 二进制；后续构建自动携带许可与源码说明。

- `optimization-review-2026-09-05.md` 记录基于 `39964da` 的本地交互性能审查、确定性验证与未覆盖范围。本轮不修改 0.2.3 版本号、不发标签；默认测试通过不代表在线模型和多屏设备已全部重新实测。合成 UI 回放与租约检查工具位于 `tests/InteractionHarness`，运行输出只放 `.codex-build`。

- 功能实景图与视频介绍只放在英文和中文 README，分别使用自然语言介绍同一组功能；发行日志只列版本更新，不重复功能图库或 GitHub Release 标题。
- README 首屏使用实际标注图，以使用场景串联五张配图和 GIF；构建命令折叠展示。明确作者、MPL-2.0 与数据边界，不能恢复商用另行授权。
- README 按当前代码列全部可用渠道及使用方法，不能继续只介绍 API/Hermes。模型效果、电脑环境差异用简短实用提示说明，不列研发验证记录，也不承诺所有模型均可用。MiniMax Code 不得被宣传为官方公开 Agent SDK。
- README 使用居中品牌与下载区、原位标注大图、两列功能图库及内嵌循环 GIF；不再提供有声 MP4 入口。GIF 从作者视频转换，保持比例和完整时长，解码验证帧延迟与循环；不加入产品依赖。GitHub About 是单一描述字段，维护简洁中英双语，而非宣称会随浏览器语言切换。
- v0.2.0 的五张 JPG 及视频由用户在发布请求中明确指定并授权公开。除此以外不得上传实际桌面、提示词、设置、日志或 QA 产物；`patent-research-2026-09-02.md` 为用户本地文件，禁止加入发布提交。
- `docs/images/ai-checkmarks.jpg`、`in-place-translation.jpg`、`ai-drawing.jpg`、`code-explanation.jpg`、`web-annotations.jpg` 保留用户提供的原图，双语说明描述能力而非保证模型正确。
- 用户视频原编码为 HEVC；演示素材已转换为仓库内 GIF，仅用于 README 展示。源 MP4 不再纳入仓库、安装包或 GitHub Release。
- 用户明确要求演示只使用 README 内嵌 GIF。Release 只上传安装 EXE 与便携 ZIP，禁止再次附带演示 MP4 或 SHA256SUMS.txt；GitHub 资产页提供原生 SHA-256 digest。应用 EXE/ZIP 也不得携带文档图片和视频。
- README 配图与 GIF 使用仓库内相对路径，不链接 Release 演示 MP4；README、安装包、csproj 与发布说明必须版本一致。
- GitHub Community Standards 由根目录的 `CODE_OF_CONDUCT.md`、`CONTRIBUTING.md`、`SECURITY.md`、`SUPPORT.md` 以及 `.github` 下的 Issue/PR 模板覆盖；README 中提供中英文入口。
- 自包含发布前必须单独按 `Release`、`x64`、`win-x64`、`SelfContained=true` 执行锁定还原，再以同样参数 `publish --no-restore`。普通解决方案还原不会保证下载 .NET/WPF Runtime pack；本机已有缓存会掩盖此问题，需用全新包目录验证。v0.2.0 在 CI 因 NETSDK1112 失败，保留原标签不移动，正式包改由 v0.2.1 发布。
- NuGet 自定义包目录不保证 `NuGetPackageRoot` 带结尾分隔符，许可证路径必须显式加入目录分隔符；全新缓存的发布演练已覆盖该差异，不能只依赖默认用户缓存。
- `workflow_dispatch` 用于新标签前的完整云端预检：编译、测试、publish 审计和安装包打包全部照常，只跳过 GitHub Release 创建。v0.2.1 被真实录屏取消测试拦住，保留失败标签，后续发布用 v0.2.2。
- v0.2.3 包含 Hermes 启动兼容、四提供商简化配置、密钥掩码、请求体参数、统一连接提示及贴图重截图阴影修复；README 继续保留 GIF 与功能图库，发行日志仅列双语修复说明。版本更新需同步 ReleasePreparationTests 及对应发行说明测试夹具。打标签前先通过 workflow_dispatch 全流程预检，正式标签不得复用。
- GitHub Windows runner 默认英语；诊断文本测试必须按实际产品语言分别断言中英文类别，不能只匹配中文导致本机通过、CI 失败。隐私断言（不泄漏原始输出）与未知退出码不能等同安装损坏的断言仍必须保留，不能通过强制 CI 中文或删测试绕过。

- 2026-09-08 MiniMax M3 视频 422 已用本机临时录屏做脱敏重放定位：约 28 秒、8.4 MB、1128×632、H.264/AAC，原文件、重新编码文件和去音轨文件均被服务端返回 HTTP 422；同参数的合成视频通过。错误 JSON 只有 `error.type` 与 `error.message`，没有服务代码或字段，消息只表明 video/content，不能安全回显。产品应将此类响应归为视频内容拒绝并提示换无敏感信息的短视频或切换渠道，不要继续误导为大小限制；Hermes 的 `minimax-cn` 视频工具仍可能先失败再走其备用 Provider。

- 2026-09-08 原位翻译必须按 OCR 源行逐项映射。解析可兼容一层常见包装和双重 JSON，但保持严格 JSON，不得接受缺行、重排行或非空源行对应的空译文。流式结果无法按原行数解析时只允许用同一批源文本自动重试一次，第二次失败只显示固定可操作提示；不得记录或显示原始模型输出。

- `teaching-mode-2026-09-05.md` 记录用户明确要求教学共享而新增的正式 `TeachingMode` 例外。验收使用 Release 合成窗口、GDI 像素与 Windows.Graphics.Capture，不得把这些本机证据写成腾讯会议/极域远端实测通过。

- `interaction-latency-2026-09-05.md` 记录用户要求恢复纯箭头、长回答回放的性能前后对比及未解决的峰值/全量录屏回归失败；不得用 P95 改善宣称所有卡顿消失。

- 用户已明确要求发布本轮优化，0.2.4 发布准备包含教学共享、增量渲染与资源回收、纯箭头布局修正及设置打开自动更新检查。早期审查文档中的“不发布/保持 0.2.3”仅描述当时阶段，不再限制此次发布；仍须先运行云端 workflow_dispatch 全流程预检，再为相同提交创建全新标签。

- v0.2.4 全流程预检 33976455443 通过，但正式标签运行 33976742529 的 RecordsSmallRegionToRealMp4 在 GIF 导出抛 ArgumentException（759/760），未创建 Release。保留该标签，不移动或复用；后续 0.2.5 为录制测试补齐 RecordingReady 同步与源时长断言，继续执行完整预检和正式标签构建。

- v0.2.5 已于 2026-09-06 发布成功：提交 35b509d，预检 33977163276 与正式标签运行 33977455091 全流程通过，本地 Release 全量 760/760。GitHub latest 指向 v0.2.5，安装 EXE、便携 ZIP、无 BOM SHA256SUMS.txt 与既有演示 MP4 均已上传；两个包的 GitHub 资产 SHA-256 与校验文件一致。v0.2.4 失败标签保留，未创建对应 Release。

- 2026-09-06 用户要求移除 v0.2.5 的 MewuAI-video-annotations.mp4 资产；同步删除中英文 README 的 MP4 入口及 release.yml 的 MP4 上传项，保留现有 GIF。既有安装 EXE、ZIP 和校验文件不得重新构建或替换，不为文档与附件清理重打版本标签。

- 2026-09-06 用户要求改用 GitHub 自带 SHA-256。更新器已支持仅安装包资产 digest，无需 SHA256SUMS.txt；后续 Release 只上传安装 EXE 与便携 ZIP。
- v0.2.6 是原生 GitHub digest 更新器的迁移发布，整合录屏音频与 MP3 导出、Hermes 安装器启动交接、历史复制/收纳、后台探测与取色优化及标注直接编辑。双语 README 指向 GitHub 原生 SHA-256，不上传演示 MP4。发布说明和安装版本统一 0.2.6，先完成相同提交的云端全流程预检再创建新标签。
- v0.2.6 于 2026-09-06 发布：标签提交 `8888fdb`，预检 `34014657105`、正式发布 `34014864784` 均成功，本地 Release 807 项通过、1 项真实音频设备显式验收跳过。当时安装 EXE 为 92,750,527 字节、便携 ZIP 为 122,754,086 字节，并附带兼容校验文件；两个包原生 digest 与校验文件一致，未附带演示 MP4。2026-09-07 清理时删除校验附件，未修改安装包或标签。
