# 长截图现场验收窗口

这是完全离线、无用户数据、无自动化输入的 WPF 测试页面，不随产品发布。
运行 `dotnet run --project tests/ScrollCaptureHarness` 后，通过鼠标和截图覆盖层验证：

- 从第 4 张彩色卡片附近开始，进入长截图后无需额外点击底层窗口即可滚动。
- 单格、连续三格向下滚轮，随后向上滚动，预览逐步拼接；反向回到已截范围不重复增长。
- 页面平滑滚动总时长 260ms，用来暴露旧版 500ms 轮询加 280ms 延迟造成的漏帧。
- 底层窗口有焦点时按 Esc，仍应取消长截图、恢复选区、长截图和录屏按钮。
- 完成后保持原图比例；复制/保存的像素高度与采集进度一致。

顶部显示真实滚动偏移。若画面偏移没有改变，应排查焦点/输入路由，而非放宽图像匹配阈值。

2026-09-04 首轮回归只核对了尺寸与滚动，未拦住 Debug 防捕获豁免把蓝色选区边框拼入导出图，不能将该轮作为成品视觉通过的证据。

修复后重新验收：175% 缩放，1415 × 900 物理像素选区；从偏移 360 DIP 开始，向下三格到 1080 DIP，长图为 2160px；向上四格到 120 DIP，增长为 2580px。完成后工具条恢复长截图与录屏入口，显示按原比例缩放的结果；保存对话框默认使用“截图_日期_时间.png”。实际打开保存 PNG 检查，尺寸为 1415 × 2580，卡片 002–008 连续，接缝和四边没有蓝色选区框、光晕、工具条或保存对话框。现场系统指针的蓝色特效仍有局部残影，不将这项验收描述为屏蔽所有第三方叠加层。验收图只保留在本地忽略目录，不随仓库或安装包发布。

采集期间即使开启 Debug 界面观察开关，也必须强制实际防捕获；完成后才恢复界面可观察。底层焦点下 Esc 取消和反向回到已覆盖范围不重复增长另有前轮现场与单元测试覆盖。
